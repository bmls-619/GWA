@model IEnumerable<GradeWebApp.Models.Role>

@{
    ViewBag.Title = "Index";
}

@*<h2>Index</h2>*@

@*@Html.Partial("_RoleMenu")*@
@Html.Partial("_IndexLayout")

<div class="panel panel-primary">

    <div class="panel-heading">Students</div>

    <div class="panel-body">

        <div class="dataTable_wrapper">

            <div class="panel-body ">
                @Html.ActionLink("Add Role", "Create", "Role", null, new { @class = "btn btn-primary" })
            </div>
                <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                    <thead>
                        <tr>
                            <th>
                                @Html.LabelFor(model => model.First().RoleId, "Role ID")
                            </th>
                            <th>
                                @Html.LabelFor(model => model.First().RoleName, "Role Name")
                            </th>
                            <th></th>
                        </tr>
                    </thead>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.RoleId)
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.RoleName)
                            </td>

                            <td>
                                @Html.Partial("_ActionMenuLayout")

                                <li>@Html.ActionLink("Edit", "Edit", new { id = item.RoleId })</li>
                                <li>@Html.ActionLink("Details", "Details", new { id = item.RoleId })</li>
                                @*@Html.ActionLink("Delete", "Delete", new { id = item.RoleId })*@

                                @Html.Partial("_ActionMenuLayout2")
                            </td>
                        </tr>
                    }

                </table>

                @Html.Partial("_IndexLayout2")

                @*<th>
                @Html.DisplayNameFor(model => model.Description)
            </th>*@

                @*<td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>*@
